# Copy to .env ON THE DEPLOY HOST (paperless.local) and fill in the real codes.
# The real .env must never land in this OneDrive-synced folder.
# Current codes are on each printer's screen (Settings -> WLAN) and in the
# OctoEverywhere compose on paperless (~/docker/octoeverywhere/docker-compose.yml).
P1S_ACCESS_CODE=xxxxxxxx
A1_ACCESS_CODE=xxxxxxxx
